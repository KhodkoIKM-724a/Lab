# Lab
#include &lt;iostream>  using namespace std; int main() { int number, product, a1, a2, a3, a4; cout &lt;&lt; "Input: "; cin >> number; a1 = number / 1000; a2 = (number / 100) % 10; a3 = (number / 10) % 10; a4 = number % 10; product = a1 * a2 * a3 * a4; cout &lt;&lt; "Input: " &lt;&lt; product &lt;&lt; endl; }
